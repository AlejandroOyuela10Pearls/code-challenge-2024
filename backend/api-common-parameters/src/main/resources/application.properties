spring.application.name=api-common-parameters

server.port=8088
server.servlet.context-path=/common-parameter

# Configuraci�n de MongoDB
spring.data.mongodb.uri=mongodb+srv://${MONGO_DB_USERNAME}:${MONGO_DB_PASSWORD}@${MONGO_DB_HOST}/${MONGO_DB_NAME}

# Consola de administraci�n de MongoDB
spring.data.mongodb.database=codeChallenge

